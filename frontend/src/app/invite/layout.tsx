/**
 * Public layout for invitation acceptance page
 * No navbar, no sidebar - clean standalone page like onboarding
 */

export default function InviteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
